import { Text } from 'react-native';

const NoResults = () => (
	<Text className='NoResults has-black-color flex-1 items-center justify-center text-center italic'>
		no words found
	</Text>
);

export default NoResults;
